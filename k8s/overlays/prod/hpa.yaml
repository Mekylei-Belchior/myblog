apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: myblog-api-hpa
  namespace: myblog
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: myblog-api
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
