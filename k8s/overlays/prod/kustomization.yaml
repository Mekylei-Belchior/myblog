apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: myblog

resources:
  - namespace.yaml
  - ../../base
  - postgres.yaml
  - hpa.yaml
  - pdb.yaml

configMapGenerator:
  - name: myblog-config
    envs:
      - config.env

patches:
  - path: patch.yaml
    target:
      kind: Deployment
      name: myblog-api
  - path: ingress-patch.yaml

images:
  - name: registry.lab.home/pessoal/myblog/api
    newTag: latest
  - name: registry.lab.home/pessoal/myblog/client
    newTag: latest
